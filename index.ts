export enum BodyIdentifiers {
  Main = "main",
  Blog = "blog",
  Music = "music",
  NotFound = "notfound"
}

export enum FaderType {
  In = "gray-fader-in",
  Out = "gray-fader-out",
}

export enum RootPages {
  Index = "/",
  Blog = "/blog",
  Music = "/music"
}